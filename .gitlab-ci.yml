stages:
  - build

before_script:
  - echo 'deb http://http.debian.net/debian jessie contrib non-free' >> /etc/apt/sources.list
  - apt-get update
  - apt-get install -y haskell-stack

stack_build:
  stage: build
  script: stack build --install-ghc
